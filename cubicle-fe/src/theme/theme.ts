import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#3B49DF' },
    secondary: { main: '#828D95' },
    background: { default: '#F4F6F8' },
  },
  shape: { borderRadius: 12 },
});