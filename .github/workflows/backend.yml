name: Deploy Backend to Infomaniak

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      HOST_USER: ${{ vars.HOST_USER }}
      HOST_REPOSITORY: ${{ vars.HOST_REPOSITORY }}
      FLASK_ENV: ${{ vars.FLASK_ENV }}
      HOST_EMAIL: ${{ vars.HOST_EMAIL }}

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Login to GitHub Container Registry
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      # 3️⃣ Build & push Docker image (backend only)
      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./cubicle-be
          push: true
          tags: |
            ghcr.io/${{ vars.HOST_USER }}/${{ vars.HOST_REPOSITORY }}:latest
            ghcr.io/${{ vars.HOST_USER }}/${{ vars.HOST_REPOSITORY }}:${{ github.sha }}

      # 4️⃣ Install kubectl
      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      # 5️⃣ Configure kubeconfig (persist across steps)
      - name: Configure kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG_DATA }}" | base64 -d > $HOME/.kube/config
          kubectl get nodes

      # 6️⃣ Create / update GHCR imagePullSecret
      - name: Create imagePullSecret
        run: |
          kubectl create secret docker-registry ghcr-secret \
            --docker-server=ghcr.io \
            --docker-username=${{ github.actor }} \
            --docker-password=${{ secrets.GHCR_PAT }} \
            --docker-email=${{ vars.HOST_EMAIL }} \
            --dry-run=client -o yaml | kubectl apply -f -

      # 7️⃣ Apply Kubernetes manifests (create Deployment & Service)
      - name: Apply backend Kubernetes manifests
        run: |
          kubectl apply -f k8s/backend.yaml

      # 8️⃣ Update Deployment image & rollout
      - name: Update backend image
        run: |
          kubectl set image deployment/backend \
            backend=ghcr.io/${{ vars.HOST_USER }}/${{ vars.HOST_REPOSITORY }}:${{ github.sha }}

          kubectl rollout status deployment/backend
